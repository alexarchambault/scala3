package build.scripted

import scala3build.*

import mill.*

trait Runner extends CrossScala3Module {
  def moduleDeps = Seq(
    build.library()
  )
}

object `package` extends DefaultTaskModule {
  def defaultTask(): String = "runScriptedTests"
  def runScriptedTests(): Task.Command[Unit] = Task.Command {
    ???
  }

  object runner extends Cross[Runner](Mode.allValues)
}
